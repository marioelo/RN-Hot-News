import React from 'react';
import {
    Text,
    SafeAreaView,
} from 'react-native';

const NewsScreen = () => {

    return (
        <SafeAreaView>
            <Text style={{textAlign: "center"}}>News Screen</Text>
        </SafeAreaView>
    );
}


export default NewsScreen;